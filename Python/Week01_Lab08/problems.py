# # Write a function to find all common elements between two lists.

def common_elements(nums1, nums2):
    same_numbers = []
    for num in nums1 and num in nums2:
        if num == True:
            same_numbers.append(num)
        print(same_numbers)
        
 
# print(common_elements([1, 2, 3], [2, 3, 4])) # [2, 3]

# '''
# when i type in a list, I want the function to compare the values contained within the lists

# 1. Iterate over nums1 and nums2 at the same time 
# 2. 
# '''
nums1 = [1,2,3,4,5,]

